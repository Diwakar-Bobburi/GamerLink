const express = require('express');
const router = express.Router();

// Placeholder for achievement routes
router.get('/', (req, res) => {
  res.send('Achievement route');
});

module.exports = router;
